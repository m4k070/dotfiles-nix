== NixOS rebuild

create symbolic link to `/etc/nixos`

----
sudo nixos-rebuild switch --flake .#
----

== Folders

hosts :: machine specify settings

home :: home-manager settings

nixos :: base nixOS setting
